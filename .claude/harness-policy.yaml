# Harness STEP policy — Task 8 (AC-8, D-005).
#
# `harness_verifier.verify_steps()` 가 본 yaml 을 읽어 각 STEP 의 마커 존재 +
# 코드 블록 부재 여부를 검증. STEP 추가 시 코드 변경 없이 yaml 만 수정.
#
# 마커 형식: <!-- HARNESS:STEP-X.Y -->
# 코드 블록: 마커 이후 4 KB 이내에 ``` (어떤 언어든) 시작 fenced block 존재 필수.

version: 1
required_steps:
  - id: STEP-2.6
    marker: "<!-- HARNESS:STEP-2.6 -->"
    file: ".claude/commands/handoff.md"
    description: "Compliance MCP/API audit submission (D-003)"

  - id: STEP-2.7
    marker: "<!-- HARNESS:STEP-2.7 -->"
    file: ".claude/commands/handoff.md"
    description: "Mempalace memory persistence (backfill + mine)"

  - id: STEP-RPI-1A
    marker: "<!-- HARNESS:STEP-RPI-1A -->"
    file: ".claude/commands/rpi.md"
    description: "superpowers availability check (Phase 1 wiring follow-up Task 4)"

  - id: STEP-RPI-2D
    marker: "<!-- HARNESS:STEP-RPI-2D -->"
    file: ".claude/commands/rpi.md"
    description: "Brainstorm Mode entry — hybrid brainstorm() invocation"

  - id: STEP-RPI-2C
    marker: "<!-- HARNESS:STEP-RPI-2C -->"
    file: ".claude/commands/rpi.md"
    description: "Plan Mode entry — hybrid write_plan() invocation"
